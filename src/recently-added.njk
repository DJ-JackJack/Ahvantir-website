---
layout: base
title: Recently Added
description: "All lore articles ordered by the date they were added to the archive."
permalink: /recently-added/
---

<div class="page-header">
  <div class="container">
    <h1 class="page-title">Recently Added</h1>
    <p class="page-lead">All lore articles, newest additions first.</p>
  </div>
</div>

<div class="container recently-container">

  {% set groups = collections.allArticlesByDateAdded | groupByField("date_added") %}

  {% if groups.length == 0 %}
  <div class="recently-empty">
    <p>No articles found in the archive yet.</p>
    <p><a href="/articles/">Browse all articles →</a></p>
  </div>

  {% else %}

  {% for group in groups %}
  <section class="recently-section">
    <h2 class="recently-section__title">
      {% if group.key == "Unknown" %}
        Undated
      {% else %}
        {{ group.key | dateDisplay }}
      {% endif %}
      <span class="recently-count">{{ group.items.length }}</span>
    </h2>
    <div class="article-grid">
      {% for article in group.items %}
      <a href="{{ article.url }}" class="article-card">
        <span class="article-card__category" style="color:
          {%- for cat in meta.categories -%}
            {%- if cat.slug == article.data.category -%}{{ cat.color }}{%- endif -%}
          {%- endfor -%}
        ">{{ article.data.category or "uncategorized" }}</span>
        <h3 class="article-card__title">{{ article.data.title }}</h3>
        {% if article.data.description %}
        <p class="article-card__desc">{{ article.data.description }}</p>
        {% endif %}
      </a>
      {% endfor %}
    </div>
  </section>
  {% endfor %}

  {% endif %}

</div>
